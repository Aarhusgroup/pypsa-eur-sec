__default__:
    partition: q64,q32,q36,q24
    name: "sector.{rule}.{wildcards}"
    output: "logs/cluster/slurm-%j-%x.{rule}.{wildcards}.out"
    error: "logs/cluster/slurm-%j-%x.{rule}.{wildcards}.err"
